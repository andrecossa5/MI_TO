singularity { 
    enabled = true   
    runOptions = ' --bind /data/ '
    cacheDir = ' ./.singularity/ '
}

process {   
    container = 'docker://acox1/mito_benchmark:1.4'  

    // Resources 
    // maxForks = min((total_CPUs / CPUs_per_job) // wait_time, queue_size)   
    cpus = 6 // Temporary
    memory = '5 G' // Temporary
}

trace {
    enabled = true
    file = './trace.txt'
}
